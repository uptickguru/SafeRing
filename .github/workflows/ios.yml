name: iOS Build & TestFlight Upload

on:
  push:
    branches: [main]

jobs:
  build-and-upload:
    name: Build & Upload to TestFlight
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test
        run: echo "iOS workflow is alive" && sw_vers
